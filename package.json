{
  "name": "job-posting-app",
  "version": "1.0.0",
  "description": "Full-stack Job Posting Web Application",
  "private": true,
  "scripts": {
    "dev": "node app.js dev",
    "start": "node app.js",
    "start:prod": "node app.js prod",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start:server": "cd backend && node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["job", "posting", "react", "express", "mongodb"],
  "author": "",
  "license": "ISC"
}

